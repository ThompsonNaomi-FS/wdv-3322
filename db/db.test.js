// bd functions (connect, disconnect, findUser, saveUser)
// User model
// mongoose

beforeEach(async () => {
 // call connect
});

describe( "", () => {
    resizeTo("find user", () => {
        // create user
        // saveUser(user)
        //expects - fname, lname
    });

    test("find user", () => {
        // get user from findUser({obj})
        // expect - user fname, lname
    });
});

afterEach(async () => {
    // call disconnect
});